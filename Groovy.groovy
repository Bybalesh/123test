def printHelloWorld(times) {
    (1..times).each {
        println 'Hello World'
    }
}
(1..times).each {
        println 'Hello World'
printHelloWorld(5)


    def printHelloWorld(times) {
    (1..times).each {
        println 'Hello World'

(1..times).each {
        println 'Hello World'
printHelloWorld(5)
